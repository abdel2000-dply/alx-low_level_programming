CC = gcc
SRC = main.c school.c
OBJ = ${SRC:.c=.o}
NAME = school
RM = rm -f

%.o: %.c m.h
		${CC} -c -o $@ $<

all: ${OBJ}
		${CC} ${OBJ} -o ${NAME}

clear:
		${RM} *~ ${NAME}

oclear:
		$(RM) $(OBJ)

fclean: clean oclean

re: oclean all
